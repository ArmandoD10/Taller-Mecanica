<?php
require_once __DIR__ . "/conexion.php";

function registrarEvento($id_usuario, $tipo) {
    // global $conexion;

    // // IP fija como pediste
    // $ip = "192.168.1.100";

    // $sql = "INSERT INTO Auditoria_Log (id_usuario, ip_equipo, tipo, fecha) 
    //         VALUES (?, ?, ?, now())";

    // $stmt = $conexion->prepare($sql);
    // $stmt->bind_param("iss", $id_usuario, $ip, $tipo);

    // $stmt->execute();
}